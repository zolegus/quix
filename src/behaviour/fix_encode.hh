//
///////////////////////////////////////////////////////////////////////////////////////////////////
//
#ifndef GUARD__BEHAVIOUR__FIX_ENCODE__HH
#define GUARD__BEHAVIOUR__FIX_ENCODE__HH
//
///////////////////////////////////////////////////////////////////////////////////////////////////
//
#include <behaviour/fix_encode.h>
//
///////////////////////////////////////////////////////////////////////////////////////////////////
//
template< typename E > 
class behaviour::fix_encode
{
  struct impl;
  impl *pimpl;

public:

  using event_type = E;

  fix_encode( void* );
  fix_encode( const fix_encode& ) = delete;
  fix_encode( fix_encode&& ) = delete;
  fix_encode& operator=( fix_encode ) = delete;
  fix_encode& operator=( fix_encode&& ) = delete;
  ~fix_encode();

  void operator()( event_type& );
  bool post();
};
//
///////////////////////////////////////////////////////////////////////////////////////////////////
//
#endif // GUARD__BEHAVIOUR__FIX_ENCODE__HH
//
///////////////////////////////////////////////////////////////////////////////////////////////////
//
